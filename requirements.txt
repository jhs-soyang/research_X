# Versions used to produce the statistics and figures reported in the manuscript
# (Python 3.9.6). Pinned so a fresh install reproduces the same numbers.
numpy==2.0.2
pandas==2.3.3
scipy==1.13.1
matplotlib==3.9.4
tabulate==0.9.0
